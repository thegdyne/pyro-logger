{
  "name": "pyro-logger",
  "version": "1.1.0",
  "description": "Pyrotechnic Work Experience Logger - PWA",
  "scripts": {
    "build": "echo 'Static HTML - no build needed'",
    "serve": "npx serve www -l 3000",
    "serve:https": "npx serve www -l 3000 --ssl-cert cert.pem --ssl-key key.pem",
    "sync": "npx cap sync",
    "ios": "npx cap open ios",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "serve": "^14.0.0"
  }
}
